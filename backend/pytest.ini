[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: uses a real SQLite database file; the Gemini test also calls the real API (needs GEMINI_API_KEY)
filterwarnings =
    ignore::DeprecationWarning
